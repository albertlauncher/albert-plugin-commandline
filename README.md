# Albert plugin: Commandline

## Features

Search and execute commands in PATH.
Specify optional arguments. 
The "Feeling lucky" item available in the triggered query handler executes any commandline (For e.g. scripts or aliases).

Item actions:
- Run in terminal
- Run in terminal and close on exit
- Run in background (without terminal)

